@model Garden_Centre_MVC.ViewModels.Transactions.HistoricViewModel

<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <td>Transaction Number</td>
            <td>Date</td>
            <td>Customer</td>
            <td>Total Value</td>
            <td>Actions</td>
        </tr>
    </thead>
    <tbody>
        @foreach (Garden_Centre_MVC.Models.TransactionOverview to in Model.TransactionOverviews)
        {
            <tr>
                <td>@to.TransactionNumber</td>
                <td>@to.Date</td>
                <td>@to.Customer.FullName</td>
                <td>@to.TotalValue</td>
                <td>
                    <button transactionNumber="@to.TransactionNumber" class="viewBtn btn btn-primary">View</button>
                    <button class="btn btn-primary">Edit</button>
                    <button class="btn btn-primary">Delete</button>
                </td>
            </tr>
            <tr id='@("Historic_ExtendedViewTR"+@to.TransactionNumber)' style="display:none; padding:0px;">
                <td id='@("Historic_ExtendedViewTD"+@to.TransactionNumber)' colspan="5">
                    <div id='@("Historic_ExtendedView"+@to.TransactionNumber)' hidden="hidden">

                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>


<script type="text/javascript">
    var prevOpenedTN = '0';
    $(document).ready(function () {
        $(".viewBtn").on("click", function (e) {
            e.preventDefault();
            var transactionNumber = $(this).attr("transactionNumber");
            $.ajax(
            {
                type: "POST",
                data: ({ _transactionNumber: transactionNumber }),
                url: '@Url.Action("ExtendedView", "Transactions")',
                success: function (result) {
                    $("#Historic_ExtendedViewTR" + transactionNumber).attr("style", "display:table-row;");
                    $("#Historic_ExtendedView" + transactionNumber).delay(100).slideDown("slow").html(result);

                    if (prevOpenedTN !== '0' && prevOpenedTN != transactionNumber) {
                        $("#Historic_ExtendedView" + prevOpenedTN).delay(100).slideUp("slow", "swing", function (e) {
                            $("#Historic_ExtendedViewTR" + prevOpenedTN).attr("style", "display:none;");
                            prevOpenedTN = transactionNumber;
                        });
                    }
                    else 
                    {
                        prevOpenedTN = transactionNumber;
                    }
                    
                },
                error: function (x, e, r) {
                    alert(e + " " + r);
                    alert(x.responseText);
                }
            });
        });
    });
</script>